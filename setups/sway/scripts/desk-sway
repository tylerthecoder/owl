#!/usr/bin/env bash
set -euo pipefail

desks_dir="$HOME/.config/desks-sway"
mkdir -p "$desks_dir"

selected=$(ls "$desks_dir" 2>/dev/null | fzf --height=30 --layout=reverse --prompt="Select Sway Desk: ")
[[ -z "${selected:-}" ]] && exit 0

"$desks_dir/$selected"



